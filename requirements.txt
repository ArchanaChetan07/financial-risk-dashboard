# Core runtime used by scripts + CI. Heavy viz/ML extras are optional.
numpy>=1.26,<2.1
pandas>=2.0,<2.3
scikit-learn>=1.3
matplotlib>=3.7
seaborn>=0.12
yfinance>=0.2.28
mysql-connector-python>=8.0.31
sqlalchemy>=2.0
python-dotenv>=1.0
# Optional local tooling (not required for leakage-safe retrain)
# sweetviz
# xgboost
# lightgbm
# catboost
# tableau-api-lib  # no 0.3.0 on PyPI for py3.10; install latest if needed
